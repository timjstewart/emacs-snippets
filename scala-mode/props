# name: class T extends Suite { .. }
# key: props
# --
import org.scalacheck._
import org.scalacheck.Prop._
import org.scalacheck.Arbitrary._

object ${1:name}Test extends Properties("$1") {
  property("$2") = forAll { $3 =>
    $0
  }
}